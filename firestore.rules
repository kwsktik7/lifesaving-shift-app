rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 認証済みユーザーは全コレクションの読み書き可
    // (管理者/学生の区別はクライアントのsessionStorageで管理)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
